import {Head} from '@inertiajs/react'; import {BarChart,Bar,XAxis,YAxis,Tooltip,ResponsiveContainer} from 'recharts'; import Layout from '../../Components/Layout';
export default function Index({statistics,daily,magnitudes}){return <Layout><Head title="Estadísticas"/><h1 className="text-4xl font-black">Radiografía de la actividad</h1><p className="text-slate-400 mt-2 mb-6">Indicadores calculados dinámicamente sobre la base local.</p><div className="grid md:grid-cols-3 gap-4">{[['Magnitud promedio',statistics.averageMagnitude],['Profundidad promedio',`${statistics.averageDepth} km`],['Mayor magnitud · 7 días',statistics.max7d]].map(([k,v])=><div className="panel p-5" key={k}><div className="label">{k}</div><div className="value mt-2">{v}</div></div>)}</div><div className="grid lg:grid-cols-2 gap-5 mt-5"><Chart title="Sismos por día" data={daily} x="date"/><Chart title="Sismos por magnitud" data={magnitudes} x="bucket"/></div><div className="panel p-6 mt-5"><h2 className="text-xl font-bold mb-5">Departamentos con mayor actividad sísmica</h2>{statistics.departments.map((d,i)=><div className="flex items-center gap-4 py-3 border-t border-slate-800" key={d.department}><span className="text-slate-500 w-5">{i+1}</span><b className="grow">{d.department}</b><span className="text-orange-400 font-bold">{d.total}</span></div>)}</div></Layout>};function Chart({title,data,x}){return <div className="panel p-6"><h2 className="font-bold mb-5">{title}</h2><ResponsiveContainer width="100%" height={280}><BarChart data={data}><XAxis dataKey={x} stroke="#789"/><YAxis stroke="#789"/><Tooltip/><Bar dataKey="total" fill="#f97316" radius={[6,6,0,0]}/></BarChart></ResponsiveContainer></div>}
